/* eslint-env node, mocha */
const expect = require('chai').expect;

describe('Line-chart component', () => {
  it('should be tested', () => {
    expect((2 / 2) === (1 / 1)).to.equal(true);
  });
});
